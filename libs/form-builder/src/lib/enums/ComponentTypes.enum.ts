export enum ComponentTypes {
  AUTO_COMPLETE = 'auto-complete',
  CHECKBOX = 'checkbox',
  CHIPS = 'chips',
  DATE = 'date',
  DATE_TIME = 'date-time',
  INPUT = 'input',
  LABEL = 'label',
  RADIO = 'radio',
  SELECT = 'select',
  TEXTAREA = 'textarea',
  TIME = 'time',
}
